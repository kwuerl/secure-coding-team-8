\subsection{Nikto}
Nikto is an open source command line web server scanner (\path{https://cirt.net/Nikto2}) which scans for multiple vulnerabilities. It lists URLs with directory indexing and URLs in general, which might be interesting. The standard command used is \code{nikto -h http://URL}.